package com.example.demo_chatbot.repository;

import com.example.demo_chatbot.entity.DocumentSync;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DocumentSyncRepository extends JpaRepository<DocumentSync,Integer> {
}
